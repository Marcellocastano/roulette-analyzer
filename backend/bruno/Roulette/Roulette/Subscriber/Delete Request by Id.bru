meta {
  name: Delete Request by Id
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/v1/subscription/request/:requestId
  body: json
  auth: bearer
}

params:path {
  requestId: 
}

auth:bearer {
  token: {{token}}
}
