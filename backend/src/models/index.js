const User = require('./user.model');
const Spin = require('./spin.model');
const Statistics = require('./statistics.model');
const InitialStats = require('./initial-stats.model');

module.exports = {
  User,
  Spin,
  Statistics,
  InitialStats
};
